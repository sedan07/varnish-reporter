# Varnish Response Time Reporter

A very basic golang program to read VSM data via varnishncsa, pull out timing information and report to statsd

**Warning**

Currently all values are hard-coded, this is WIP

